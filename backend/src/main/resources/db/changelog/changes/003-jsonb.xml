<?xml version="1.0" encoding="UTF-8"?>


<!--
 ============================================================================
 TICKET-ADV009 — JSONB column on instruments (+ GIN index on Postgres)
 WHAT:    instruments.metadata as JSONB to hold per-asset-class detail
          (e.g. dividend yield, country, sector).
 HOW:     Postgres gets a true JSONB column and GIN index; H2 gets a
          compatible text column so the schema still matches the entity.
 WHY:     Schemaless metadata avoids 12 columns that only apply to one asset
          class. Trade-off: validation moves to the app layer.
 OBSERVE: \d instruments shows metadata jsonb on Postgres and a text column
          on H2. EXPLAIN ANALYZE on a metadata @> containment query hits
          idx_instruments_metadata_gin on Postgres.
 ============================================================================
-->
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

   <changeSet id="003-jsonb-on-instruments" author="trainer">

    <preConditions onFail="MARK_RAN">
        <not>
            <columnExists
                tableName="instruments"
                columnName="metadata"/>
        </not>
    </preConditions>

    <sql dbms="postgresql">
        ALTER TABLE instruments
        ADD COLUMN metadata JSONB DEFAULT '{}'::JSONB NOT NULL;

        CREATE INDEX idx_instruments_metadata_gin
        ON instruments USING GIN (metadata jsonb_path_ops);
    </sql>

    <sql dbms="h2">
        ALTER TABLE instruments
        ADD COLUMN metadata CLOB DEFAULT '{}' NOT NULL;
    </sql>

    <rollback>
        <sql dbms="postgresql">
            DROP INDEX IF EXISTS idx_instruments_metadata_gin;
        </sql>
        <sql>
            ALTER TABLE instruments DROP COLUMN metadata;
        </sql>
    </rollback>

</changeSet>

</databaseChangeLog>
